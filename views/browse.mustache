{{{header}}}

<div class="row-fluid">
  <div class="span12">
      <h1>_("Browse all libraries")</h1>
      <p>_("Listing totally {{count}} libraries.")</p>
      <ul  class="nav nav-pills nav-stacked">
      {{#libraries}}
        <li><a 
        {{#additional_info.slug}}
        href="/{{additional_info.slug}}"
        {{/additional_info.slug}}
        {{^additional_info.slug}}
        href="/id/{{id}}"
        {{/additional_info.slug}}
        id="{{id}}">{{name_fi}} (tyyppi {{organisation_type}}{{^additional_info.slug}}, slug-kenttä puuttuu{{/additional_info.slug}})</a></li>
      {{/libraries}}
      </ul>
  </div>
</div>

{{{footer}}}
