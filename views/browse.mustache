{{{header}}}

<div class="row-fluid">
  <div class="span12">
      <h1>_("Browse all libraries")</h1>
      <p>_("Listing total of {{count}} libraries.")</p>
      <ul  class="nav nav-pills nav-stacked">
      {{#libraries}}
        <li><a 
        {{#additional_info.slug}}
        href="{{additional_info.slug}}"
        {{/additional_info.slug}}
        {{^additional_info.slug}}
        href="{{id}}"
        {{/additional_info.slug}}
        id="{{id}}">
		{{#name__("locale")}}
			{{name__("locale")}}
		{{/name__("locale")}}
		{{^name__("locale")}}
			{{name_fi}}
		{{/name__("locale")}}
		
		<!-- TODO: change color depending on org type -->
		<span class="label" style="background-color:#0a0";>{{organisation_type}}</span> 
		{{#consortium}}<span class="label" style="background-color:#00a";>{{consortium}}</span>{{/consortium}}
		{{^additional_info.slug}}<span class="label label-important">_("slug missing")</span>{{/additional_info.slug}}</a></li>
      {{/libraries}}
      </ul>
  </div>
</div>

{{{footer}}}
